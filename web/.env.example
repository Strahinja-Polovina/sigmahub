# Required in production — session signing key (min 32 chars, high entropy).
# Generate with: npx auth secret  OR  openssl rand -base64 32
BETTER_AUTH_SECRET=

# Optional: path for PGlite data directory (dev only)
# PGLITE_DIR=.data/pg

# Optional: enable SQL query logging (dev only)
# SQL_LOG=1
